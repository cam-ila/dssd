<%= form_for(incident_replacement) do |f| %>
  <% if incident_replacement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(incident_replacement.errors.count, "error") %> prohibited this incident_replacement from being saved:</h2>

      <ul>
      <% incident_replacement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.number_field :amount %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
